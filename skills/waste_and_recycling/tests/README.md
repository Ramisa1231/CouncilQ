# Tests

Deterministic tests for helper scripts and contracts will live here.

Use tests for:

- JSON schema validity.
- Source allowlist checks.
- PII masking helper behavior.
- Policy decision type checks.

Do not assert exact natural-language LLM output in pytest.

