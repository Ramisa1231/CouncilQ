{
  "type": "object",
  "required": [
    "decision",
    "reason",
    "sanitized_input",
    "requires_approval",
    "allowed_tool"
  ],
  "properties": {
    "decision": {
      "type": "string",
      "enum": [
        "allow",
        "sanitize_and_continue",
        "requires_human_approval",
        "block"
      ]
    },
    "reason": {
      "type": "string"
    },
    "sanitized_input": {
      "type": "string"
    },
    "requires_approval": {
      "type": "boolean"
    },
    "allowed_tool": {
      "type": "boolean"
    },
    "redactions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}

