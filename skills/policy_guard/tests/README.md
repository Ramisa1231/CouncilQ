# Tests

Deterministic tests for `policy_guard` helpers will live here.

Use tests for:

- Redaction behavior.
- Policy decision schema validity.
- Environment and role tool checks.

Do not assert exact LLM wording in pytest.

