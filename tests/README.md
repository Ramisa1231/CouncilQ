# Tests

Project-level deterministic tests will live here.

Use pytest for:

- Policy helper behavior.
- JSON schema validation.
- Source allowlist checks.
- Ingestion metadata checks.

Do not assert exact LLM response wording in pytest.

